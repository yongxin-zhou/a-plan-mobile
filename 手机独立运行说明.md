# A计划 - 手机独立运行说明

## 概述

本指南帮助你在 Android 手机上独立运行 A计划 时间管理应用，无需依赖电脑。

---

## 前置条件

- Android 手机（Android 7.0+）
- 存储空间 100MB+
- 稳定的 WiFi 连接（安装时需要）

---

## 第一步：安装 Termux

### 1.1 下载 Termux

**重要：不要从 Google Play 下载，版本太旧！**

从 F-Droid 下载：
- 地址：https://f-droid.org/packages/com.termux/
- 直接下载 APK：https://f-droid.org/repo/com.termux_1000.apk

### 1.2 安装 Termux

1. 下载 APK 文件
2. 在手机上打开 APK 文件
3. 允许安装未知来源应用
4. 完成安装

### 1.3 配置 Termux

打开 Termux，运行以下命令：

```bash
# 更新系统
pkg update && pkg upgrade

# 安装存储访问权限
termux-setup-storage
```

---

## 第二步：安装 Python 环境

在 Termux 中运行：

```bash
# 安装 Python
pkg install python

# 安装 pip
pip install --upgrade pip

# 安装项目依赖
pip install fastapi uvicorn sqlalchemy jinja2 httpx python-multipart aiofiles
```

---

## 第三步：传输项目文件

### 方法 A：使用电脑传输（推荐）

1. 在电脑上安装 ADB（Android Debug Bridge）
2. 手机开启 USB 调试
3. 连接手机到电脑
4. 运行以下命令：

```bash
# 创建项目目录
adb shell mkdir -p /data/data/com.termux/files/home/a-plan

# 传输文件
adb push "D:\MyCode\A计划\项目\时间管理软件\a_plan_mobile.py" /data/data/com.termux/files/home/a-plan/
adb push "D:\MyCode\A计划\项目\时间管理软件\static" /data/data/com.termux/files/home/a-plan/
adb push "D:\MyCode\A计划\项目\时间管理软件\templates" /data/data/com.termux/files/home/a-plan/
```

### 方法 B：使用 Termux 文件共享

1. 在 Termux 中运行：
   ```bash
   termux-setup-storage
   ```

2. 允许 Termux 访问存储

3. 将项目文件复制到手机的 `Download` 文件夹

4. 在 Termux 中运行：
   ```bash
   # 复制文件到项目目录
   cp ~/storage/downloads/a_plan_mobile.py ~/a-plan/
   cp -r ~/storage/downloads/static ~/a-plan/
   cp -r ~/storage/downloads/templates ~/a-plan/
   ```

### 方法 C：使用 Git

如果项目已上传到 GitHub：

```bash
cd ~/a-plan
git clone https://github.com/your-username/a-plan.git .
```

### 方法 D：使用 SCP（从电脑传输）

```bash
# 在电脑上运行
scp "D:\MyCode\A计划\项目\时间管理软件\a_plan_mobile.py" your-username@localhost:~/a-plan/
scp -r "D:\MyCode\A计划\项目\时间管理软件\static" your-username@localhost:~/a-plan/
scp -r "D:\MyCode\A计划\项目\时间管理软件\templates" your-username@localhost:~/a-plan/
```

---

## 第四步：启动应用

在 Termux 中运行：

```bash
cd ~/a-plan
python a_plan_mobile.py
```

看到以下输出表示启动成功：

```
==================================================
A计划 - 时间管理（移动端）
==================================================
服务地址: http://127.0.0.1:8000
按 Ctrl+C 停止服务器
==================================================
```

---

## 第五步：访问应用

1. 打开手机浏览器（Chrome、Firefox 等）

2. 地址栏输入：
   ```
   http://127.0.0.1:8000
   ```

3. 即可使用 A计划 应用

---

## 第六步：添加到桌面（可选）

### 方法 A：使用 Termux:Widget

1. 安装 Termux:Widget（从 F-Droid 下载）

2. 在 Termux 中创建启动脚本：
   ```bash
   mkdir -p ~/.shortcuts
   cat > ~/.shortcuts/start-a-plan.sh << 'EOF'
   #!/bin/bash
   cd ~/a-plan
   python a_plan_mobile.py
   EOF
   chmod +x ~/.shortcuts/start-a-plan.sh
   ```

3. 长按手机桌面 → 添加小部件 → 选择 Termux:Widget

4. 桌面上会出现快捷方式，点击即可启动

### 方法 B：使用 PWA

1. 在手机浏览器中打开 `http://127.0.0.1:8000`

2. 点击浏览器菜单 → 添加到主屏幕

3. 桌面上会出现应用图标

---

## 使用说明

### 首次使用

1. 打开应用后，点击"登录"

2. 点击"注册账号"

3. 输入用户名和密码

4. 登录后即可使用

### 功能说明

- **周计划**：规划每周的时间安排
- **年度规划**：设置年度目标
- **伴侣聊天**：与伴侣沟通（需绑定）
- **经期追踪**：记录和预测经期
- **更多功能**：在"更多"菜单中

### 绑定伴侣

1. 登录后，进入"设置"

2. 查看你的"邀请码"

3. 让伴侣也注册账号

4. 在"绑定伙伴"处输入对方的邀请码

5. 绑定成功后即可使用伴侣功能

---

## 常见问题

### Q: 启动时报错 "Address already in use"

A: 端口被占用，修改端口号：
```bash
python a_plan_mobile.py --port 8001
```

### Q: 无法访问 http://127.0.0.1:8000

A: 检查服务器是否启动，或者尝试使用手机的 IP 地址：
```bash
# 查看手机 IP 地址
ifconfig

# 使用 IP 地址访问
http://192.168.1.100:8000
```

### Q: 数据库错误

A: 删除数据库重新开始：
```bash
rm ~/a-plan/time_manager.db
python a_plan_mobile.py
```

### Q: 如何停止服务器

A: 在 Termux 中按 `Ctrl+C`

### Q: 如何后台运行

A: 使用 tmux：
```bash
pkg install tmux
tmux new -s aplan
cd ~/a-plan
python a_plan_mobile.py
# 按 Ctrl+B，然后按 D 分离会话
# 重新连接：tmux attach -t aplan
```

---

## 备份数据

数据库文件在 `~/a-plan/time_manager.db`

备份方法：
```bash
# 复制到手机存储
cp ~/a-plan/time_manager.db ~/storage/shared/a-plan-backup.db
```

恢复方法：
```bash
# 从手机存储恢复
cp ~/storage/shared/a-plan-backup.db ~/a-plan/time_manager.db
```

---

## 更新应用

1. 下载最新版本的 `a_plan_mobile.py`

2. 替换旧文件：
   ```bash
   cp ~/storage/downloads/a_plan_mobile.py ~/a-plan/
   ```

3. 重启服务器

---

## 技术支持

如有问题，请检查：

1. Termux 版本是否最新
2. Python 依赖是否安装完整
3. 项目文件是否完整
4. 端口是否被占用

---

## 下一步

1. ✅ 安装 Termux
2. ✅ 安装 Python 环境
3. ✅ 传输项目文件
4. ✅ 启动应用
5. 🔄 根据使用反馈继续优化
